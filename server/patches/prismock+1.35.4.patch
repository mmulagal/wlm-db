diff --git a/node_modules/prismock/build/main/lib/operations/find/find.js b/node_modules/prismock/build/main/lib/operations/find/find.js
index 6a72534..90d5a4a 100644
--- a/node_modules/prismock/build/main/lib/operations/find/find.js
+++ b/node_modules/prismock/build/main/lib/operations/find/find.js
@@ -20,7 +20,13 @@ function findNextIncrement(properties, fieldName) {
     return increment;
 }
 function findOne(args, current, delegates) {
-    const found = (0, helpers_1.pipe)((items) => items.filter((item) => where(args.where, current, delegates)(item)), order(args, current, delegates), connect(args, current, delegates), paginate(args.skip, args.take))(current.getItems()).at(0);
+    // wlmdb patch: add cursor-aware pagination support (Prisma parity)
+    const found = (0, helpers_1.pipe)(
+        (items) => items.filter((item) => where(args.where, current, delegates)(item)),
+        order(args, current, delegates),
+        connect(args, current, delegates),
+        paginate(args.skip, args.take, args.cursor)
+    )(current.getItems()).at(0);
     if (!found)
         return null;
     return structuredClone(select(args.select)(found));
@@ -114,11 +120,34 @@ function order(args, delegate, delegates) {
         return o;
     };
 }
-function paginate(skip, take) {
+function paginate(skip, take, cursor) {
     return (items) => {
-        if (!skip && !take)
+        // wlmdb patch: cursor-based pagination implementation
+        // Behavior mirrors Prisma: if cursor present, find index of cursor record, then apply skip/take relative to that position.
+        if (!skip && !take && !cursor)
             return items;
-        return items.slice(skip !== null && skip !== void 0 ? skip : 0, take === undefined ? undefined : take + (skip !== null && skip !== void 0 ? skip : 0));
+
+        let startIndex = skip ?? 0;
+
+        if (cursor) {
+            // cursor shape expected: { id: value } or any unique field(s)
+            // Find first item matching all cursor fields
+            const cursorKeys = Object.keys(cursor);
+            const cursorIdx = items.findIndex((it) => cursorKeys.every((k) => it[k] === cursor[k]));
+            if (cursorIdx !== -1) {
+                // Prisma semantics: if skip not provided but cursor provided, default skip is 1 (to move past cursor)
+                if (skip == null) {
+                    startIndex = cursorIdx + 1;
+                } else {
+                    startIndex = cursorIdx + skip;
+                }
+            }
+        }
+
+        if (take === undefined) {
+            return items.slice(startIndex);
+        }
+        return items.slice(startIndex, startIndex + take);
     };
 }
 function includes(args, current, delegates) {
@@ -189,16 +218,24 @@ const getFieldRelationshipWhere = (item, field, delegates) => {
     };
 };
 exports.getFieldRelationshipWhere = getFieldRelationshipWhere;
+// wlmdb patch: support composite relation keys for cascade deletes/lookups
 const getFieldFromRelationshipWhere = (item, field) => {
-    return {
-        [field.relationFromFields[0]]: item[field.relationToFields[0]],
-    };
+  if (field?.relationFromFields?.length > 1 && field?.relationToFields?.length === field?.relationFromFields?.length) {
+    const obj = {};
+    field.relationFromFields.forEach((from, idx) => { obj[from] = item[field.relationToFields[idx]]; });
+    return obj;
+  }
+  return { [field.relationFromFields[0]]: item[field.relationToFields[0]] };
 };
 exports.getFieldFromRelationshipWhere = getFieldFromRelationshipWhere;
+// wlmdb patch: support composite relation keys
 const getFieldToRelationshipWhere = (item, field) => {
-    return {
-        [field.relationToFields[0]]: item[field.relationFromFields[0]],
-    };
+  if (field?.relationToFields?.length > 1 && field?.relationFromFields?.length === field?.relationToFields?.length) {
+    const obj = {};
+    field.relationToFields.forEach((to, idx) => { obj[to] = item[field.relationFromFields[idx]]; });
+    return obj;
+  }
+  return { [field.relationToFields[0]]: item[field.relationFromFields[0]] };
 };
 exports.getFieldToRelationshipWhere = getFieldToRelationshipWhere;
 function connect(args, current, delegates) {
@@ -210,7 +247,13 @@ function connect(args, current, delegates) {
     };
 }
 function findMany(args, current, delegates) {
-    const found = (0, helpers_1.pipe)((items) => items.filter((item) => where(args.where, current, delegates)(item)), order(args, current, delegates), connect(args, current, delegates), paginate(args.skip, args.take))(current.getItems());
+    // wlmdb patch: cursor-aware pagination support (Prisma parity)
+    const found = (0, helpers_1.pipe)(
+        (items) => items.filter((item) => where(args.where, current, delegates)(item)),
+        order(args, current, delegates),
+        connect(args, current, delegates),
+        paginate(args.skip, args.take, args.cursor)
+    )(current.getItems());
     if (args === null || args === void 0 ? void 0 : args.distinct) {
         const values = {};
         return found.filter((item) => {
diff --git a/node_modules/prismock/build/main/lib/operations/find/match.js b/node_modules/prismock/build/main/lib/operations/find/match.js
index 7236dbc..531f76e 100644
--- a/node_modules/prismock/build/main/lib/operations/find/match.js
+++ b/node_modules/prismock/build/main/lib/operations/find/match.js
@@ -75,6 +75,7 @@ const matchMultiple = (item, where, current, delegates) => {
                 if (info === null || info === void 0 ? void 0 : info.relationName) {
                     const childName = (0, helpers_1.camelize)(info.type);
                     let childWhere = {};
+                    
                     if (filter.every) {
                         childWhere = filter.every;
                     }
@@ -84,12 +85,40 @@ const matchMultiple = (item, where, current, delegates) => {
                     else if (filter.none) {
                         childWhere = filter.none;
                     }
+                    else if (filter.is && typeof filter.is === 'object') {
+                        childWhere = filter.is;
+                    }
                     else {
                         childWhere = filter;
                     }
-                    const res = delegates[childName]
-                        .getItems()
-                        .filter(matchFnc(Object.assign(Object.assign({}, childWhere), (0, find_1.getFieldRelationshipWhere)(item, info, delegates)), delegates[childName]));
+                    const relationshipWhere = (0, find_1.getFieldRelationshipWhere)(item, info, delegates);
+                    const finalWhere = Object.assign(Object.assign({}, childWhere), relationshipWhere);
+                    const allChildItems = delegates[childName].getItems();
+                    const res = allChildItems.filter(matchFnc(finalWhere, delegates[childName]));
+                    
+                    // Check if this current record is actually linked to the filtered child records
+                    const isCurrentRecordLinked = () => {
+                        if (res.length === 0) return false;
+                        
+                        // Get the composite foreign key from current record
+                        const currentRecordForeignKey = {};
+                        if (info.relationFromFields && info.relationFromFields.length > 0) {
+                            info.relationFromFields.forEach((fromField, idx) => {
+                                const toField = info.relationToFields[idx];
+                                currentRecordForeignKey[toField] = item[fromField];
+                            });
+                        }
+                        
+                        // Check if any of the filtered child records matches the current record's foreign key
+                        const isLinked = res.some(childRecord => {
+                            return Object.keys(currentRecordForeignKey).every(key => 
+                                childRecord[key] === currentRecordForeignKey[key]
+                            );
+                        });
+                        
+                        return isLinked;
+                    };
+                    
                     if (filter.every) {
                         if (res.length === 0)
                             return false;
@@ -97,7 +126,7 @@ const matchMultiple = (item, where, current, delegates) => {
                         return res.length === all.length;
                     }
                     else if (filter.some) {
-                        return res.length > 0;
+                        return isCurrentRecordLinked();
                     }
                     else if (filter.is === null) {
                         return res.length === 0;
@@ -105,7 +134,7 @@ const matchMultiple = (item, where, current, delegates) => {
                     else if (filter.none) {
                         return res.length === 0;
                     }
-                    return res.length > 0;
+                    return isCurrentRecordLinked();
                 }
                 const compositeIndex = current.model.uniqueIndexes.map((index) => index.name).includes(child) ||
                     ((_a = current.model.primaryKey) === null || _a === void 0 ? void 0 : _a.name) === child ||
diff --git a/node_modules/prismock/build/module/lib/operations/find/find.js b/node_modules/prismock/build/module/lib/operations/find/find.js
index 8771706..934a53a 100644
--- a/node_modules/prismock/build/module/lib/operations/find/find.js
+++ b/node_modules/prismock/build/module/lib/operations/find/find.js
@@ -7,7 +7,13 @@ export function findNextIncrement(properties, fieldName) {
     return increment;
 }
 export function findOne(args, current, delegates) {
-    const found = pipe((items) => items.filter((item) => where(args.where, current, delegates)(item)), order(args, current, delegates), connect(args, current, delegates), paginate(args.skip, args.take))(current.getItems()).at(0);
+    // wlmdb patch: add cursor-aware pagination support (Prisma parity)
+    const found = pipe(
+        (items) => items.filter((item) => where(args.where, current, delegates)(item)),
+        order(args, current, delegates),
+        connect(args, current, delegates),
+        paginate(args.skip, args.take, args.cursor)
+    )(current.getItems()).at(0);
     if (!found)
         return null;
     return structuredClone(select(args.select)(found));
@@ -100,11 +106,27 @@ export function order(args, delegate, delegates) {
         return o;
     };
 }
-export function paginate(skip, take) {
+export function paginate(skip, take, cursor) {
     return (items) => {
-        if (!skip && !take)
+        // wlmdb patch: cursor-based pagination implementation
+        if (!skip && !take && !cursor)
             return items;
-        return items.slice(skip ?? 0, take === undefined ? undefined : take + (skip ?? 0));
+        let startIndex = skip ?? 0;
+        if (cursor) {
+            const cursorKeys = Object.keys(cursor);
+            const cursorIdx = items.findIndex((it) => cursorKeys.every((k) => it[k] === cursor[k]));
+            if (cursorIdx !== -1) {
+                if (skip == null) {
+                    startIndex = cursorIdx + 1; // default skip past cursor
+                } else {
+                    startIndex = cursorIdx + skip;
+                }
+            }
+        }
+        if (take === undefined) {
+            return items.slice(startIndex);
+        }
+        return items.slice(startIndex, startIndex + take);
     };
 }
 export function includes(args, current, delegates) {
@@ -170,15 +192,23 @@ export const getFieldRelationshipWhere = (item, field, delegates) => {
         [field.relationToFields[0]]: item[field.relationFromFields[0]],
     };
 };
+// wlmdb patch: support composite relation keys for cascade deletes/lookups
 export const getFieldFromRelationshipWhere = (item, field) => {
-    return {
-        [field.relationFromFields[0]]: item[field.relationToFields[0]],
-    };
+  if (field?.relationFromFields?.length > 1 && field?.relationToFields?.length === field?.relationFromFields?.length) {
+    const obj = {};
+    field.relationFromFields.forEach((from, idx) => { obj[from] = item[field.relationToFields[idx]]; });
+    return obj;
+  }
+  return { [field.relationFromFields[0]]: item[field.relationToFields[0]] };
 };
+// wlmdb patch: support composite relation keys
 export const getFieldToRelationshipWhere = (item, field) => {
-    return {
-        [field.relationToFields[0]]: item[field.relationFromFields[0]],
-    };
+  if (field?.relationToFields?.length > 1 && field?.relationFromFields?.length === field?.relationToFields?.length) {
+    const obj = {};
+    field.relationToFields.forEach((to, idx) => { obj[to] = item[field.relationFromFields[idx]]; });
+    return obj;
+  }
+  return { [field.relationToFields[0]]: item[field.relationFromFields[0]] };
 };
 function connect(args, current, delegates) {
     return (items) => {
@@ -189,7 +219,13 @@ function connect(args, current, delegates) {
     };
 }
 export function findMany(args, current, delegates) {
-    const found = pipe((items) => items.filter((item) => where(args.where, current, delegates)(item)), order(args, current, delegates), connect(args, current, delegates), paginate(args.skip, args.take))(current.getItems());
+    // wlmdb patch: cursor-aware pagination support (Prisma parity)
+    const found = pipe(
+        (items) => items.filter((item) => where(args.where, current, delegates)(item)),
+        order(args, current, delegates),
+        connect(args, current, delegates),
+        paginate(args.skip, args.take, args.cursor)
+    )(current.getItems());
     if (args?.distinct) {
         const values = {};
         return found.filter((item) => {
diff --git a/node_modules/prismock/build/module/lib/operations/find/match.js b/node_modules/prismock/build/module/lib/operations/find/match.js
index f5d6c9b..1844be9 100644
--- a/node_modules/prismock/build/module/lib/operations/find/match.js
+++ b/node_modules/prismock/build/module/lib/operations/find/match.js
@@ -80,12 +80,40 @@ export const matchMultiple = (item, where, current, delegates) => {
                     else if (filter.none) {
                         childWhere = filter.none;
                     }
+                    else if (filter.is && typeof filter.is === 'object') {
+                        childWhere = filter.is;
+                    }
                     else {
                         childWhere = filter;
                     }
-                    const res = delegates[childName]
-                        .getItems()
-                        .filter(matchFnc(Object.assign(Object.assign({}, childWhere), getFieldRelationshipWhere(item, info, delegates)), delegates[childName]));
+                    const relationshipWhere = getFieldRelationshipWhere(item, info, delegates);
+                    const finalWhere = Object.assign(Object.assign({}, childWhere), relationshipWhere);
+                    const allChildItems = delegates[childName].getItems();
+                    const res = allChildItems.filter(matchFnc(finalWhere, delegates[childName]));
+                    
+                    // Check if this current record is actually linked to the filtered child records
+                    const isCurrentRecordLinked = () => {
+                        if (res.length === 0) return false;
+                        
+                        // Get the composite foreign key from current record
+                        const currentRecordForeignKey = {};
+                        if (info.relationFromFields && info.relationFromFields.length > 0) {
+                            info.relationFromFields.forEach((fromField, idx) => {
+                                const toField = info.relationToFields[idx];
+                                currentRecordForeignKey[toField] = item[fromField];
+                            });
+                        }
+                        
+                        // Check if any of the filtered child records matches the current record's foreign key
+                        const isLinked = res.some(childRecord => {
+                            return Object.keys(currentRecordForeignKey).every(key => 
+                                childRecord[key] === currentRecordForeignKey[key]
+                            );
+                        });
+                        
+                        return isLinked;
+                    };
+                    
                     if (filter.every) {
                         if (res.length === 0)
                             return false;
@@ -93,7 +121,7 @@ export const matchMultiple = (item, where, current, delegates) => {
                         return res.length === all.length;
                     }
                     else if (filter.some) {
-                        return res.length > 0;
+                        return isCurrentRecordLinked();
                     }
                     else if (filter.is === null) {
                         return res.length === 0;
@@ -101,7 +129,7 @@ export const matchMultiple = (item, where, current, delegates) => {
                     else if (filter.none) {
                         return res.length === 0;
                     }
-                    return res.length > 0;
+                    return isCurrentRecordLinked();
                 }
                 const compositeIndex = current.model.uniqueIndexes.map((index) => index.name).includes(child) ||
                     current.model.primaryKey?.name === child ||
