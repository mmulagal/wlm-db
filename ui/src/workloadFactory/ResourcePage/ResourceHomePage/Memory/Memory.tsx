import { Typography } from '@netapp/design-system';
import styles from './Memory.module.scss';

const Memory = () => {
    return (
        <div className={styles.memory}>
            <Typography variant="Semibold_14">Memory</Typography>
        </div>
    );
};

export default Memory;
