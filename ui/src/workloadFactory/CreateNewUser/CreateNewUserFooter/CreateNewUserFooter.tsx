import { Button } from '@netapp/design-system';

const CreateNewUserFooter = () => {
    return (
        <>
            <Button isThin onClick={() => {}}>
                {'Create'}
            </Button>
            <Button isThin variant="secondary" onClick={() => {}}>
                {'Close'}
            </Button>
        </>
    );
};

export default CreateNewUserFooter;
