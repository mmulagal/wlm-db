import { createSlice, PayloadAction } from '@reduxjs/toolkit';

const initialState: any = {};

const postgressSlice = createSlice({
    name: 'postgress',
    initialState,
    reducers: {}
});

export const {} = postgressSlice.actions;
export default postgressSlice;
