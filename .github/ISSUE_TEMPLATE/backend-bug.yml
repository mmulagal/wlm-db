name: Backend Bug Report
description: Report a bug related to backend/API functionality.
title: "[BUG][API]: "
labels: ["Bug", "Backend"]
assignees:
  - shaswatinetapp
projects: ["TLVeng/41"]

body:
  - type: markdown
    attributes:
      value: |
        ## 🐞 Backend/API Bug Report

        Thank you for taking the time to help us improve! Please fill out the details below as thoroughly as possible.

  - type: textarea
    id: summary
    attributes:
      label: "Bug Summary"
      description: "Describe the bug and what you expected to happen."
      placeholder: "Describe the bug and what you expected to happen. Provide proper steps to reproduce the issue."
    validations:
      required: true

  - type: dropdown
    id: severity
    attributes:
      label: "Severity/Impact"
      description: "How severe is this issue?"
      options:
        - "Critical (blocks all work)"
        - "Major (significant impact)"
        - "Minor (minor inconvenience)"
        - "Trivial (cosmetic or low impact)"
    validations:
      required: true

  - type: textarea
    id: curl
    attributes:
      label: "cURL Command"
      description: "Provide a cURL command that reproduces the issue (if applicable)."
      placeholder: |
        curl -X ...
    validations:
      required: false

  - type: dropdown
    id: environment
    attributes:
      label: "Environment"
      description: "Which environment did you encounter the bug in?"
      options:
        - "staging"
        - "prod"
        - "staging-demo"
        - "prod-demo"
    validations:
      required: true

  - type: textarea
    id: attachments
    attributes:
      label: "Attachments"
      description: "Attach any relevant files (logs, HAR files, etc.)."
      placeholder: "Drag and drop files here."